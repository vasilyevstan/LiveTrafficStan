name: Validation

on:
  pull_request:
    branches:
      - dev
      - main
  push:
    branches:
      - dev
      - main

permissions:
  contents: read

concurrency:
  group: validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npm run typecheck

      - name: Test
        run: npm test -- --run

      - name: Check aircraft metadata
        run: npm run check:aircraft-metadata

      - name: Check country allocations
        run: npm run check:country-allocations

      - name: Check vessel photos
        run: npm run check:vessel-photos
        env:
          VESSEL_PHOTO_IMMUTABLE_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}

      - name: Check ports
        run: npm run check:ports

      - name: Check airports
        run: npm run check:airports

      - name: Build
        run: npm run build

      - name: Check deployment bundle
        run: npm run check:deploy
        env:
          WRANGLER_SEND_METRICS: false

      - name: Build PWA retirement bundle
        run: npm run build:pwa-retire

      - name: Check PWA retirement deployment bundle
        run: npm run check:deploy
        env:
          WRANGLER_SEND_METRICS: false
